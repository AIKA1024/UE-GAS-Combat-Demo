# 战斗架构说明（GAS + 连段树）

> 记录当前（2026-08）战斗系统架构，供阅读/改动用。
> 涉及：GAS 能力基类、**连段树**（左/右键分支）、预输入缓冲、翻滚保留连段、动画 Notify。
> 文件根目录：`Source/First/Public|Private/`。

---

## 一、总览

```
输入层        FT_PlayerController（Enhanced Input → Primary/Roll/Secondary）
                ↓
连段状态层    UFT_ComboComponent（挂在 AFT_PlayerCharacter 上） + UFT_ComboDefinition（连段树）
                ↓ 按"当前节点 + 输入"查树跳招
能力层        UFT_ComboGamePlayAbility（连段GA，按节点分类） / UFT_GamePlayAbility（翻滚等通用）
                ↓ 播放蒙太奇
动画层        AnimMontage + 各 AnimNotify（窗口/预输入/命中）
                ↓ 回调
GAS          UGameplayAbility / UAbilitySystemComponent / FGameplayTag
```

- 连段状态（`CurrentNodeTag`）存在 **组件** 上，与单个招式 GA 的生命周期解耦 → 翻滚/取消只结束当前招式，不清连段进度；超时/受击才清。
- 连段由 **树** 定义：每个"前缀路径"是一个节点，节点存"本招 GA + 按左/右键去哪个节点"。**支持 xyyx 等任意左/右键混合连段，且可多起手（左→X、右→Y…）**。

---

## 二、能力基类

### `UFT_GamePlayAbility`（通用基类）
`Public/AbilitySystem/Abilities/FT_GamePlayAbility.h`

- 只留通用内容：`bDrawDebugs`（激活时屏幕调试信息）。
- **不含任何连段逻辑**。翻滚等非连段能力继承它。

### `UFT_ComboGamePlayAbility`（连段特化）
`Public/AbilitySystem/Abilities/FT_ComboGamePlayAbility.h`

- 继承 `UFT_GamePlayAbility`。
- `ComboNodeTag`（FGameplayTag）——本招在连段树里的节点 tag（须与树里该节点的 `NodeTag` 一致）。
- `ActivateAbility` 覆写：`Super` 后，若 `ComboNodeTag` 有效，从 `AvatarActor` 找 `UFT_ComboComponent` → `NotifyComboActivated(ComboNodeTag)`。
- `EndAbility` 覆写：`Super` 后，找组件 → `NotifyComboEnded(ComboNodeTag, bWasCancelled)`。

---

## 三、连段树定义：`UFT_ComboDefinition`

`Public/AbilitySystem/Combat/UFT_ComboDefinition.h`（UDataAsset，每角色一个实例）

```cpp
USTRUCT FFT_ComboNode
{
    FGameplayTag NodeTag;                          // 节点标识
    TSubclassOf<UFT_ComboGamePlayAbility> Ability; // 本招 GA
    TMap<FGameplayTag, FGameplayTag> NextByInput;  // 输入tag(Primary/Secondary) → 下一节点tag
};
UCLASS UFT_ComboDefinition : UDataAsset
{
    TMap<FGameplayTag,FGameplayTag> StartNodesByInput; // 输入(Primary/Secondary)→起手节点（左→X、右→Y）
    TArray<FFT_ComboNode> Nodes;                   // 所有节点
    const FFT_ComboNode* FindNode(NodeTag) const;  // 按 tag 查节点
};
```

- 激活按 **GA 类**（`TryActivateAbilityByClass`），不需要给每个节点注册 tag 到 AbilityTags；节点 tag 只用于状态跟踪 + GA 自报。
- 例：xyyx 路径 → 树里 `X.NextByInput[Secondary]=XY`、`XY.NextByInput[Secondary]=XYY`、`XYY.NextByInput[Primary]=XYYX`。

---

## 四、连段状态：`UFT_ComboComponent`

`Public/AbilitySystem/Combat/UFT_ComboComponent.h`
挂在 `AFT_PlayerCharacter` 构造里。事件驱动，`bCanEverTick=false`。

### 字段
| 字段 | 含义 |
|---|---|
| `CurrentNodeTag` | 当前连段节点 tag（空=无连段） |
| `ComboDefinition` | 连段树（EditDefaultsOnly，在 BP 的角色组件上设） |
| `bComboAbilityActive` | 是否有连段 GA 激活中 |
| `bComboWindowOpen` | **任意 `FT_ComboWindowState` 窗口**（连段招/翻滚）是否开着（接招门槛） |
| `bPerInputOpen` | 预输入窗口是否开 |
| `BufferedInputTag` | 缓冲的输入 tag（空=无缓冲；记住按的是左还是右） |
| `ComboResetTimer` / `ComboResetDelay` | 宽限/重置定时器（默认 2s） |

### 方法
| 方法 | 调用者 | 作用 |
|---|---|---|
| `RequestAttack(InputTag)` | 控制器 `Primary()`/`Secondary()`、缓冲触发 | 攻击输入入口：按当前节点+输入查树跳招 |
| `NotifyComboActivated(NodeTag)` | 连段 GA `ActivateAbility` | `CurrentNodeTag=NodeTag; bActive=true; 清宽限; window=false` |
| `NotifyComboEnded(NodeTag, bWasCancelled)` | 连段 GA `EndAbility` | `NodeTag != CurrentNodeTag` 忽略；否则 `bActive=false; 开宽限; 处理缓冲` |
| `NotifyComboWindowOpened()` | 窗口 notify Begin（连段招/翻滚） | `bComboWindowOpen = true`；`FlushBufferedAttack()` |
| `NotifyComboWindowClosed()` | 窗口 notify End | `bComboWindowOpen=false` |
| `SetPerInputOpen(bool)` | 预输入 notify | 设 `bPerInputOpen` |
| `ClearBufferedAttack()` | 翻滚/重置 | 丢弃缓冲 |
| `RefreshComboGrace()` | 控制器 `Roll()` | 延长宽限（翻滚保留连段） |
| `ResetCombo()` | 受击/死亡/手动 | 清空连段 |

### `RequestAttack` 判定
```
CurrentNodeTag 空（无连段）       → ActivateNode(StartNodesByInput[Input])（起手不要求窗口）
bComboWindowOpen（连段招/翻滚窗口）→ Next=树查(Current, Input)；有效则 ActivateNode(Next)，无跳转忽略
bPerInputOpen（预输入窗口）       → BufferAttack(InputTag)，窗口开时触发
其他（无窗口也无预输入窗口）      → 忽略（严格：必须进窗口才接）
```

> ⚠️ 接招门槛用 `bComboWindowOpen`（组件标志，由**任何** `FT_ComboWindowState` 置位——连段招或翻滚蒙太奇），**不是** loose tag `FFTag::Windows.Combo`。`GE_RollCombo` 等加的 tag 不影响门槛。

---

## 五、动画 Notify（AnimNotify）

### `UFT_ComboWindowState`（组合窗口，状态 notify）
- `NotifyBegin`：挂 loose tag `FFTag::Windows.Combo` → `NotifyComboWindowOpened()`。
- `NotifyEnd`：摘 tag → `NotifyComboWindowClosed()`。
- 放在连段蒙太奇上，定义"这招的出招/可取消窗口"；**也可放在翻滚蒙太奇上**，作为翻滚的接招窗口（只有窗口内才能接下一招）。

### `UFT_PerInputWindowState`（预输入接受窗口）
- `NotifyBegin/End` → `SetPerInputOpen(true/false)`。
- 只定义"这段时间按攻击会被接受/缓冲"；提前按下的输入在**连段窗口打开**或**本招收招**时自动触发（`BufferedInputTag` 记住按了左/右）。

### `UFT_Serath_PrimaryAttackState`（命中检测）
- `NotifyTick` 球体扫射，命中发 `FFTag.Events.Player.PrimaryTraceHit`。

### `UFT_ComboWindow`（瞬时"刷新"notify）—— **见"不再需要的类"**

---

## 六、输入：`FT_PlayerController`

| 输入 | 处理 |
|---|---|
| `Primary()` | `Combo->RequestAttack(FFTag.Abilities.Primary)` |
| `Secondary()` | `Combo->RequestAttack(FFTag.Abilities.Secondary)`（右键=连段分支/起手输入） |
| `Roll()` | `ClearBufferedAttack()` + `RefreshComboGrace()` + 激活翻滚 |

> 右键（Secondary）**不是独立攻击**：空闲时只有 `StartNodesByInput` 配了 Secondary→某节点才会起手（Y 起手），否则忽略；连段中只作分支跳转输入。

## 七、GA 资产配置

### 连段 GA（父类 `UFT_ComboGamePlayAbility`）
每个连段树节点一个 GA：
- `ComboNodeTag` = 该节点 tag（如 `FFTag.Combo.X`、`.XY`、`.XYX`…）。
- `AbilityTags` = `{Primary, Secondary, nodeTag}`；`CancelAbilitiesWithTag` = `{Primary, Secondary}`（取消上一段）；`ActivationBlockedTags` = `{own nodeTag}`（**不含 Roll**——翻滚窗口内可激活下一招）。
- 蒙太奇上要有 `UFT_ComboWindowState`（窗口）、`UFT_PerInputWindowState`（预输入）、`UFT_Serath_PrimaryAttackState`（命中）。
- **激活走 `TryActivateAbilityByClass`（由树里的节点引用 GA 类）**，不再用 `TryActivateAbilitiesByTag(段tag)`。

现有 4 个连段 GA（X/XX/XXX/XXXX）已迁移：`ComboNodeTag = FFTag.Combo.X/XX/XXX/XXXX`。
分支 GA `GA_Serath_Combo_XY/XYY/XYYX` 已脚手架（占位蒙太奇）；Y 起手 GA 待建。

### GA_ActionRoll（父类 `UFT_GamePlayAbility`）
| 项 | 值 |
|---|---|
| AbilityTags | Roll |
| ActivationOwnedTags | **（空）** —— tag 永不残留 |
| ActivationRequiredTags | **Window.Combo** —— 只有连段窗口内才能翻滚 |
| CancelAbilitiesWithTag | Primary, Secondary —— 翻滚取消攻击，但**攻击不取消翻滚** |

---

## 八、核心流程

> 每个箭头的具体方法调用、判定顺序、状态位变化见 **§九 调用链详解（方法级）**。

### 普通连段（节奏）
```
按左键 → RequestAttack(Primary) → CurrentNode空 → ActivateNode(StartNodesByInput[Primary]=X) → GA_X 激活
  → NotifyComboActivated(X)        [CurrentNodeTag=X, bActive=true]
  → 蒙太奇播到窗口 → NotifyComboWindowOpened  [bComboWindowOpen=true]
  → 窗口内按左键 → 树查 X.NextByInput[Primary]=XX → ActivateNode(XX)
```

### xyyx 分支（左、右、右、左）
```
X →(右) 树查 X.NextByInput[Secondary]=XY → GA_XY
  →(右) XY.NextByInput[Secondary]=XYY → GA_XYY
  →(左) XYY.NextByInput[Primary]=XYYX → GA_XYYX
```

### 多起手（Y）
```
右键（空闲）→ RequestAttack(Secondary) → CurrentNode空 → ActivateNode(StartNodesByInput[Secondary]=Y) → GA_Y
```
- 起手节点完全由 `StartNodesByInput` 表决定；`Primary→X` 必配，`Secondary→Y` 选配。
- 未配 Secondary 时右键单独按**不会攻击**。

### 预输入（提前按）
```
连段中、窗口未开但预输入窗口开 → 按左/右 → BufferAttack(InputTag)
  → 窗口一开 → NotifyComboWindowOpened → RequestAttack(BufferedInputTag) → 自动接下一段
  → 或本招收招 → NotifyComboEnded → 触发缓冲
```

### 翻滚（窗口接招 + 预输入，严格）
```
连段中按翻滚 → Roll(): ClearBufferedAttack + RefreshComboGrace + 激活翻滚
  → 翻滚的 CancelAbilitiesWithTag 取消当前连段 GA → NotifyComboEnded → 开宽限(2s)
  → 翻滚蒙太奇的 FT_PerInputWindowState 打开 → 提前按攻击 → RequestAttack 缓冲（窗口未开时）
  → 翻滚蒙太奇的 FT_ComboWindowState 打开 → NotifyComboWindowOpened → bComboWindowOpen=true + FlushBufferedAttack → 接下一招
  → 翻滚结束（Roll tag 归零）→ OnRollTagChanged 丢弃残留缓冲（窗口没触发过的输入不再生效）
  → 窗口没按、翻滚结束 → 严格不接（宽限只保连段数）；超宽限 → OnComboReset → 清空（重新起手）
```

---

## 九、调用链详解（方法级）

> 把「输入 → 激活 → 预输入 → 翻滚」每一步展开到**具体方法调用**。字段/方法速查见 §四，流程概览见 §八。
> 以下调用顺序均对应当前源码（`UFT_ComboComponent.cpp`、`FT_PlayerController.cpp`、`FT_ComboGamePlayAbility.cpp`、两个 notify）。

### 9.1 方法速查（谁调用谁、做了什么）

| 方法 | 位置 | 调用者 | 做了什么 |
|---|---|---|---|
| `Primary()` / `Secondary()` | `FT_PlayerController` | Enhanced Input（Started） | 取 `UFT_ComboComponent` → `RequestAttack(FFTag.Abilities.Primary / Secondary)` |
| `Roll()` | `FT_PlayerController` | Enhanced Input（Started） | `ClearBufferedAttack()` → `RefreshComboGrace()` → 激活翻滚 |
| `RequestAttack(InputTag)` | `UFT_ComboComponent` | 控制器、`FlushBufferedAttack` | **攻击唯一入口**：按「当前节点 + 输入」查树（判定见 9.2） |
| `GetStartNodeTag(Input)` | `UFT_ComboComponent` | `RequestAttack` | 查 `StartNodesByInput[Input]`；未配置返回空 tag |
| `GetNextNodeTag(Current, Input)` | `UFT_ComboComponent` | `RequestAttack` | `FindNode(Current)->NextByInput[Input]`；无跳转返回空 tag |
| `ActivateNode(NodeTag)` | `UFT_ComboComponent` | `RequestAttack` | `FindNode(NodeTag)` → `TryActivateAbilityByClass(Node->Ability, false)`；死亡 tag 守卫 |
| `ActivateAbility` | `UFT_ComboGamePlayAbility` | GAS | `Super`（打屏调试）→ `NotifyComboActivated(ComboNodeTag)` |
| `EndAbility` | `UFT_ComboGamePlayAbility` | GAS | `Super` → `NotifyComboEnded(ComboNodeTag, bWasCancelled)` |
| `NotifyComboActivated(NodeTag)` | `UFT_ComboComponent` | 连段 GA 激活 | `CurrentNodeTag=NodeTag`、活跃位=true、关窗口、清宽限 |
| `NotifyComboEnded(NodeTag, bCancelled)` | `UFT_ComboComponent` | 连段 GA 结束 | 防 stale-end → 关活跃 → 开宽限 → 取消则丢缓冲，否则触发缓冲 |
| `SetPerInputOpen(bool)` | `UFT_ComboComponent` | 预输入 notify | 设 `bPerInputOpen` |
| `NotifyComboWindowOpened()` | `UFT_ComboComponent` | 窗口 notify Begin（连段招/翻滚） | `bComboWindowOpen=true`；`FlushBufferedAttack()` |
| `NotifyComboWindowClosed()` | `UFT_ComboComponent` | 窗口 notify End | `bComboWindowOpen = false` |
| `BufferAttack(Input)` | `UFT_ComboComponent` | `RequestAttack` | `BufferedInputTag = Input`（单槽，最新覆盖旧） |
| `ClearBufferedAttack()` | `UFT_ComboComponent` | `Roll()`、组件内部 | `BufferedInputTag` 置空 |
| `FlushBufferedAttack()` | `UFT_ComboComponent` | 窗口开（连段招/翻滚）、正常收招 | 取缓冲 → 清空 → `RequestAttack(缓冲输入)` |
| `RefreshComboGrace()` | `UFT_ComboComponent` | `Roll()` | `CurrentNodeTag` 有效则重开宽限定时器（默认 2s） |
| `OnComboReset()` | `UFT_ComboComponent` | 宽限定时器 | 清缓冲、清 `CurrentNodeTag`、关活跃 |
| `BeginPlay` / `OnRollTagChanged` | `UFT_ComboComponent` | GAS tag 事件 | `RegisterGameplayTagEvent(Roll)`；翻滚结束（count→0）→ `ClearBufferedAttack()`（丢弃未在窗口触发的缓冲） |

### 9.2 `RequestAttack` 判定（逐分支）

```
0. ComboDefinition 空 或 InputTag 无效          → return（忽略）
1. CurrentNodeTag 空（无连段）→ 起手，**不要求窗口**
     Start=GetStartNodeTag(Input)；Start 有效 → ActivateNode(Start)；return
     （未配置该输入的起手 → 什么都不做，如右键在没配 Y 时）
2. CurrentNodeTag 有效（连段中 / 翻滚后宽限期）——一律严格窗口
     bComboWindowOpen（连段招或翻滚的 FT_ComboWindowState 窗口）
         Next=GetNextNodeTag(Current, Input)；Next 有效 → ClearBufferedAttack() → ActivateNode(Next)
         无跳转 → 忽略
     bPerInputOpen（预输入窗口）→ BufferAttack(Input)
     既无窗口也无预输入窗口 → 忽略（必须进窗口才接）
```

`ActivateNode` 内部：`FindNode` → 节点与 GA 类非空 → ASC 非空且无 `Status.Dead` → `TryActivateAbilityByClass(Node->Ability, false)`。

### 9.3 时序：普通连段（节奏内按左键）

```
按下左键
  FT_PlayerController::Primary()
    → Combo->RequestAttack(Primary)
        ① 无连段 → GetStartNodeTag(Primary)=X → ActivateNode(X)
           FindNode(X)->Ability = GA_Serath_Combo_X → ASC->TryActivateAbilityByClass(GA_X)
  GA_X 激活
    UFT_ComboGamePlayAbility::ActivateAbility
      → Combo->NotifyComboActivated(FFTag.Combo.X)
          CurrentNodeTag=X；bComboAbilityActive=true；bComboWindowOpen=false；清宽限
  GA_X 的蒙太奇播放
    预输入 notify Begin → Combo->SetPerInputOpen(true)
    窗口到 → UFT_ComboWindowState::NotifyBegin
      → AddLooseGameplayTags(FFTag.Windows.Combo)
      → Combo->NotifyComboWindowOpened()：bComboWindowOpen=true；FlushBufferedAttack()（无缓冲则空跑）
  窗口内再按左键
    Primary() → RequestAttack(Primary)
        CurrentNodeTag=X、活跃、窗口开 → GetNextNodeTag(X, Primary)=XX
        → ClearBufferedAttack() → ActivateNode(XX)
          → TryActivateAbilityByClass(GA_XX)
  GA_XX 激活流程
    → XX 的 CancelAbilitiesWithTag={Primary,Secondary} 命中激活中的 GA_X → 先取消 GA_X
    GA_X::EndAbility(bWasCancelled=true)
      → Combo->NotifyComboEnded(X, true)
          X==CurrentNodeTag（此刻仍是 X）→ 处理：活跃=false；关窗口；开宽限(2s)
          取消 → ClearBufferedAttack()
    GA_XX::ActivateAbility
      → Combo->NotifyComboActivated(XX)：CurrentNodeTag=XX；活跃=true；窗口=false；清宽限
  （循环：窗口再开 → 再按 → 跳 XXX …）
```

> 注意 `NotifyComboEnded` 里的 stale-end 守卫（`NodeTag != CurrentNodeTag → return`）：正常同步取消时旧段先结束、新段后激活，不触发；它防的是「旧段 EndAbility 通知晚于新段 ActivateAbility 通知」的乱序情况。

### 9.4 时序：预输入（窗口未开时提前按）

```
连段中（GA_X 激活，窗口未开，预输入窗口已开）
  提前按左键 → RequestAttack(Primary)
      活跃、窗口未开、bPerInputOpen=true → BufferAttack(Primary)   [BufferedInputTag=Primary]
  蒙太奇播到窗口 → UFT_ComboWindowState::NotifyBegin
    → NotifyComboWindowOpened()
        bComboWindowOpen=true
        FlushBufferedAttack()：取 Primary → ClearBufferedAttack() → RequestAttack(Primary)
          活跃+窗口开 → GetNextNodeTag(X, Primary)=XX → ActivateNode(XX)   （提前的输入自动续段）
  若窗口一直没开，直接收招 → GA_X::EndAbility(bWasCancelled=false)
    → NotifyComboEnded(X, false)：活跃=false；开宽限；FlushBufferedAttack()
        RequestAttack(Primary)：无窗口也无预输入窗口 → 忽略（缓冲丢弃）
  （严格：只有窗口开时预输入才触发；窗口没开则输入作废）
```

### 9.5 时序：翻滚（窗口接招 + 预输入，严格）

```
连段中（GA_X 激活）
  按翻滚 → FT_PlayerController::Roll()
    → Combo->ClearBufferedAttack()          （丢弃翻滚前缓冲）
    → Combo->RefreshComboGrace()            （CurrentNodeTag=X 有效 → 重开宽限 2s）
    → ActivateAbility(FFTag.Abilities.Roll) → TryActivateAbilitiesByTag(Roll)
  翻滚激活 → 翻滚的 CancelAbilitiesWithTag={Primary,Secondary} 取消 GA_X
    GA_X::EndAbility(true) → NotifyComboEnded(X, true)：活跃=false；开宽限；取消 → ClearBufferedAttack()
  翻滚动画中
    预输入窗口开（FT_PerInputWindowState）→ 按攻击 → RequestAttack(Primary)
        CurrentNodeTag=X、无窗口 → BufferAttack(Primary)         （预输入缓冲）
    翻滚窗口开（FT_ComboWindowState）→ NotifyComboWindowOpened
        bComboWindowOpen=true → FlushBufferedAttack() → RequestAttack(Primary)
          → GetNextNodeTag(X, Primary)=XX → ActivateNode(XX)     （翻滚窗口内接招）
        窗口内直接按 → 同样走「窗口开」分支接招
    窗口外 / 非预输入窗口按 → 忽略
  翻滚结束 → 翻滚 GA EndAbility → Roll tag 移除（count 1→0）
    OnRollTagChanged(tag, 0) → ClearBufferedAttack()（丢弃窗口没触发的残留缓冲）
  窗口没按 → 严格不接；超宽限 → OnComboReset → 重新起手
```

### 9.6 状态位一览（谁置谁清）

| 状态位 | 置位 | 清零 |
|---|---|---|
| `CurrentNodeTag` | `NotifyComboActivated` | `ResetCombo` / `OnComboReset` |
| `bComboAbilityActive` | `NotifyComboActivated` | `NotifyComboEnded` / `ResetCombo` / `OnComboReset` |
| `bComboWindowOpen` | `NotifyComboWindowOpened`（连段招/翻滚窗口） | `NotifyComboWindowClosed` / `NotifyComboActivated` / `NotifyComboEnded` |
| `bPerInputOpen` | `SetPerInputOpen(true)`（预输入 notify Begin） | `SetPerInputOpen(false)`（notify End） |
| `BufferedInputTag` | `BufferAttack` | `ClearBufferedAttack` / `FlushBufferedAttack` |
| `ComboResetTimer`（宽限） | `RefreshComboGrace` / `NotifyComboEnded` | `NotifyComboActivated` / `ResetCombo` / 到点 `OnComboReset` |

---

## 十、Tag 一览（`GamePlayTags/FTTag.h`）

| Tag | 用途 |
|---|---|
| `FFTag.Windows.Combo` | 组合窗口（loose tag）；翻滚的 ActivationRequiredTags 需要它 |
| `FFTag.Combo.X / .XX / .XXX / .XXXX` | 默认 XXXX 路径节点 tag |
| `FFTag.Combo.XY / .XYX / .XYY / .XYYX` | 分支节点 tag（示例，按需继续加） |
| `FFTag.Abilities.Primary` / `Secondary` | 左/右输入（连段树跳转的 key） |
| `FFTag.Abilities.Roll` | 翻滚 |
| `FFTag.Abilities.ActivatedOnGiven` | 给能力时自动激活（ASC 处理） |
| `FFTag.Status.Dead` | 死亡状态（激活守卫用） |
| `FFTag.Events.Player.PrimaryTraceHit` | 命中事件 |
| ~~`FFTag.Abilities.BlockHitReact`~~ / ~~`.Death`~~ | **未使用**，可删 |

---

## 十一、不再需要的类 / 资产 / Tag

| 对象 | 现状 | 建议 |
|---|---|---|
| **`GA_Roll.uasset`**（旧翻滚） | 从未给玩家，GA_ActionRoll 的旧副本 | **删除** |
| **`UFT_ComboWindow`**（瞬时 Refresh notify） | 已从所有蒙太奇移除（2026-08 扫描确认），源码里只剩空壳类，只做 `Remove+Add(窗口tag)`；节奏门槛已改读组件标志，它不再起作用 | **删除类（`FT_ComboWindow.h/.cpp`）** |
| **`GE_RollCombo`** | 翻滚完成时加 `Window.Combo` 0.5s；现在不影响节奏门槛 | **可移除**（删 GA_ActionRoll 里的施加节点 + 删资产） |
| **翻滚蒙太奇的 `UFT_ComboWindowState`** | ~~已移除~~ → 严格窗口方案下**重新加回**作接招窗口 | 加回（接招窗口）+ `FT_PerInputWindowState`（预输入） |
| `FFTag.Abilities.BlockHitReact` / `.Death` | 无能力/代码使用 | 删除 tag 定义 |
| `bCanPerInput` / `PerInputTag`（控制器） | 旧预输入残留 | **已删除** |

---

## 十二、当前待办 & 注意点

1. **连段树 DataAsset 已建**：`DA_Serath_Combo`（`UFT_ComboDefinition` 实例）已配好 XXXX 路径并跑通。剩余配置：
   - `StartNodesByInput`：**Primary→`FFTag.Combo.X` 必填**（否则无起手）；要右键起手再配 `Secondary→FFTag.Combo.Y`。
   - `Nodes` 补齐分支/起手节点（每个节点选 GA + 填 `NextByInput`）；Y 起手需先建 `GA_Serath_Combo_Y`（父类 `UFT_ComboGamePlayAbility`、`ComboNodeTag=FFTag.Combo.Y`、AbilityTags、蒙太奇 + notify）。
2. **重启编辑器前必须完整构建（DebugGame，与编辑器加载的 DLL 一致）**：
   ```
   "D:\UEEngine\UE_5.8\Engine\Build\BatchFiles\Build.bat" FirstEditor Win64 DebugGame -Project="D:\Dev\CPP\Unreal Projects\First\First.uproject" -WaitMutex
   ```
3. `ComboResetDelay`（2s）在 `UFT_ComboComponent` UPROPERTY 上，蓝图可改。
4. 翻滚的 `ActivationRequiredTags={Window.Combo}` = 中立状态翻滚不可用（战斗专用闪避），想改就删这项。
5. `GA_ActionRoll` 的 PlayMontageAndWait **四个委托全接 `EndAbility`**（翻滚被接招打断时干净收尾，Roll tag 不残留）。
6. **严格窗口（2026-08）**：所有接招必须进窗口。待编辑器重连后：
   - 去掉 7 个连段 GA 的 `ActivationBlockedTags` 里的 `Roll`（否则翻滚窗口内激活下一招被挡）——可让我用 Python 批量改，或手动在 Class Defaults 删。
   - 翻滚蒙太奇 `AM_ActionRoll_front` 加 `FT_ComboWindowState`（接招窗口）+ 调 `FT_PerInputWindowState`（预输入）位置。
