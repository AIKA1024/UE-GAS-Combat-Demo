# 战斗架构说明（GAS + 连段树）

> 记录当前（2026-08）战斗系统架构，供阅读/改动用。
> 涉及：GAS 能力基类、**连段树**（左/右键分支）、预输入缓冲、翻滚保留连段、动画 Notify。
> 文件根目录：`Source/First/Public|Private/`。

---

## 一、总览

```
输入层        FT_PlayerController（Enhanced Input → Primary/Roll/Secondary）
                ↓
连段状态层    UFT_ComboComponent（挂在 AFT_PlayerCharacter 上） + UFT_ComboDefinition（连段树）
                ↓ 按"当前节点 + 输入"查树跳招
能力层        UFT_ComboGamePlayAbility（连段GA，按节点分类） / UFT_GamePlayAbility（翻滚等通用）
                ↓ 播放蒙太奇
动画层        AnimMontage + 各 AnimNotify（窗口/预输入/命中）
                ↓ 回调
GAS          UGameplayAbility / UAbilitySystemComponent / FGameplayTag
```

- 连段状态（`CurrentNodeTag`）存在 **组件** 上，与单个招式 GA 的生命周期解耦 → 翻滚/取消只结束当前招式，不清连段进度；超时/受击才清。
- 连段由 **树** 定义：每个"前缀路径"是一个节点，节点存"本招 GA + 按左/右键去哪个节点"。**支持 xyyx 等任意左/右键混合连段，且可多起手（左→X、右→Y…）**。

---

## 二、能力基类

### `UFT_GamePlayAbility`（通用基类）
`Public/AbilitySystem/Abilities/FT_GamePlayAbility.h`

- 只留通用内容：`bDrawDebugs`（激活时屏幕调试信息）。
- **不含任何连段逻辑**。翻滚等非连段能力继承它。

### `UFT_ComboGamePlayAbility`（连段特化）
`Public/AbilitySystem/Abilities/FT_ComboGamePlayAbility.h`

- 继承 `UFT_GamePlayAbility`。
- `ComboNodeTag`（FGameplayTag）——本招在连段树里的节点 tag（须与树里该节点的 `NodeTag` 一致）。
- `ActivateAbility` 覆写：`Super` 后，若 `ComboNodeTag` 有效，从 `AvatarActor` 找 `UFT_ComboComponent` → `NotifyComboActivated(ComboNodeTag)`。
- `EndAbility` 覆写：`Super` 后，找组件 → `NotifyComboEnded(ComboNodeTag, bWasCancelled)`。

---

## 三、连段树定义：`UFT_ComboDefinition`

`Public/AbilitySystem/Combat/UFT_ComboDefinition.h`（UDataAsset，每角色一个实例）

```cpp
USTRUCT FFT_ComboNode
{
    FGameplayTag NodeTag;                          // 节点标识
    TSubclassOf<UFT_ComboGamePlayAbility> Ability; // 本招 GA
    TMap<FGameplayTag, FGameplayTag> NextByInput;  // 输入tag(Primary/Secondary) → 下一节点tag
};
UCLASS UFT_ComboDefinition : UDataAsset
{
    TMap<FGameplayTag,FGameplayTag> StartNodesByInput; // 输入(Primary/Secondary)→起手节点（左→X、右→Y）
    TArray<FFT_ComboNode> Nodes;                   // 所有节点
    const FFT_ComboNode* FindNode(NodeTag) const;  // 按 tag 查节点
};
```

- 激活按 **GA 类**（`TryActivateAbilityByClass`），不需要给每个节点注册 tag 到 AbilityTags；节点 tag 只用于状态跟踪 + GA 自报。
- 例：xyyx 路径 → 树里 `X.NextByInput[Secondary]=XY`、`XY.NextByInput[Secondary]=XYY`、`XYY.NextByInput[Primary]=XYYX`。

---

## 四、连段状态：`UFT_ComboComponent`

`Public/AbilitySystem/Combat/UFT_ComboComponent.h`
挂在 `AFT_PlayerCharacter` 构造里。事件驱动，`bCanEverTick=false`。

### 字段
| 字段 | 含义 |
|---|---|
| `CurrentNodeTag` | 当前连段节点 tag（空=无连段） |
| `ComboDefinition` | 连段树（EditDefaultsOnly，在 BP 的角色组件上设） |
| `bComboAbilityActive` | 是否有连段 GA 激活中 |
| `bComboWindowOpen` | **连段蒙太奇**的窗口是否开着（节奏门槛） |
| `bPerInputOpen` | 预输入窗口是否开 |
| `BufferedInputTag` | 缓冲的输入 tag（空=无缓冲；记住按的是左还是右） |
| `ComboResetTimer` / `ComboResetDelay` | 宽限/重置定时器（默认 2s） |

### 方法
| 方法 | 调用者 | 作用 |
|---|---|---|
| `RequestAttack(InputTag)` | 控制器 `Primary()`/`Secondary()`、缓冲触发 | 攻击输入入口：按当前节点+输入查树跳招 |
| `NotifyComboActivated(NodeTag)` | 连段 GA `ActivateAbility` | `CurrentNodeTag=NodeTag; bActive=true; 清宽限; window=false` |
| `NotifyComboEnded(NodeTag, bWasCancelled)` | 连段 GA `EndAbility` | `NodeTag != CurrentNodeTag` 忽略；否则 `bActive=false; 开宽限; 处理缓冲` |
| `NotifyComboWindowOpened()` | 窗口 notify Begin | `bComboWindowOpen = bComboAbilityActive`；若有缓冲则触发 |
| `NotifyComboWindowClosed()` | 窗口 notify End | `bComboWindowOpen=false` |
| `SetPerInputOpen(bool)` | 预输入 notify | 设 `bPerInputOpen` |
| `ClearBufferedAttack()` | 翻滚/重置 | 丢弃缓冲 |
| `RefreshComboGrace()` | 控制器 `Roll()` | 延长宽限（翻滚保留连段） |
| `ResetCombo()` | 受击/死亡/手动 | 清空连段 |

### `RequestAttack` 判定
```
CurrentNodeTag 空                → ActivateNode(StartNodesByInput[Input])（未配置忽略）
连段中 && bComboWindowOpen       → Next=树查(Current, Input); Next有效则 ActivateNode(Next)（无跳转忽略）
连段中 && 窗口未开 && bPerInputOpen → BufferAttack(InputTag)
已结束 && 翻滚中（ASC 带 Roll）  → BufferAttack(InputTag)，翻滚结束自动触发
已结束 && 宽限有效               → Next有效则 ActivateNode(Next)，否则回各自起手 StartNodesByInput[Input]
其他                            → 回各自起手 StartNodesByInput[Input]（未配置忽略）
```

> ⚠️ 节奏门槛用 `bComboWindowOpen`（组件标志，仅连段蒙太奇窗口置位），**不是** loose tag `FFTag::Windows.Combo`。翻滚蒙太奇 / `GE_RollCombo` 加的 tag 不再影响门槛。

---

## 五、动画 Notify（AnimNotify）

### `UFT_ComboWindowState`（组合窗口，状态 notify）
- `NotifyBegin`：挂 loose tag `FFTag::Windows.Combo` → `NotifyComboWindowOpened()`。
- `NotifyEnd`：摘 tag → `NotifyComboWindowClosed()`。
- 放在连段蒙太奇上，定义"这招的出招/可取消窗口"。

### `UFT_PerInputWindowState`（预输入接受窗口）
- `NotifyBegin/End` → `SetPerInputOpen(true/false)`。
- 只定义"这段时间按攻击会被接受/缓冲"；提前按下的输入在**连段窗口打开**或**本招收招**时自动触发（`BufferedInputTag` 记住按了左/右）。

### `UFT_Serath_PrimaryAttackState`（命中检测）
- `NotifyTick` 球体扫射，命中发 `FFTag.Events.Player.PrimaryTraceHit`。

### `UFT_ComboWindow`（瞬时"刷新"notify）—— **见"不再需要的类"**

---

## 六、输入：`FT_PlayerController`

| 输入 | 处理 |
|---|---|
| `Primary()` | `Combo->RequestAttack(FFTag.Abilities.Primary)` |
| `Secondary()` | `Combo->RequestAttack(FFTag.Abilities.Secondary)`（右键=连段分支/起手输入） |
| `Roll()` | `ClearBufferedAttack()` + `RefreshComboGrace()` + 激活翻滚 |

> 右键（Secondary）**不是独立攻击**：空闲时只有 `StartNodesByInput` 配了 Secondary→某节点才会起手（Y 起手），否则忽略；连段中只作分支跳转输入。

## 七、GA 资产配置

### 连段 GA（父类 `UFT_ComboGamePlayAbility`）
每个连段树节点一个 GA：
- `ComboNodeTag` = 该节点 tag（如 `FFTag.Combo.X`、`.XY`、`.XYX`…）。
- `AbilityTags` = `{Primary, Secondary, nodeTag}`；`CancelAbilitiesWithTag` = `{Primary, Secondary}`（取消上一段）；`ActivationBlockedTags` = `{own nodeTag}`。
- 蒙太奇上要有 `UFT_ComboWindowState`（窗口）、`UFT_PerInputWindowState`（预输入）、`UFT_Serath_PrimaryAttackState`（命中）。
- **激活走 `TryActivateAbilityByClass`（由树里的节点引用 GA 类）**，不再用 `TryActivateAbilitiesByTag(段tag)`。

现有 4 个连段 GA（X/XX/XXX/XXXX）已迁移：`ComboNodeTag = FFTag.Combo.X/XX/XXX/XXXX`。
分支 GA `GA_Serath_Combo_XY/XYY/XYYX` 已脚手架（占位蒙太奇）；Y 起手 GA 待建。

### GA_ActionRoll（父类 `UFT_GamePlayAbility`）
| 项 | 值 |
|---|---|
| AbilityTags | Roll |
| ActivationOwnedTags | **（空）** —— tag 永不残留 |
| ActivationRequiredTags | **Window.Combo** —— 只有连段窗口内才能翻滚 |
| CancelAbilitiesWithTag | Primary, Secondary —— 翻滚取消攻击，但**攻击不取消翻滚** |

---

## 八、核心流程

### 普通连段（节奏）
```
按左键 → RequestAttack(Primary) → CurrentNode空 → ActivateNode(StartNodesByInput[Primary]=X) → GA_X 激活
  → NotifyComboActivated(X)        [CurrentNodeTag=X, bActive=true]
  → 蒙太奇播到窗口 → NotifyComboWindowOpened  [bComboWindowOpen=true]
  → 窗口内按左键 → 树查 X.NextByInput[Primary]=XX → ActivateNode(XX)
```

### xyyx 分支（左、右、右、左）
```
X →(右) 树查 X.NextByInput[Secondary]=XY → GA_XY
  →(右) XY.NextByInput[Secondary]=XYY → GA_XYY
  →(左) XYY.NextByInput[Primary]=XYYX → GA_XYYX
```

### 多起手（Y）
```
右键（空闲）→ RequestAttack(Secondary) → CurrentNode空 → ActivateNode(StartNodesByInput[Secondary]=Y) → GA_Y
```
- 起手节点完全由 `StartNodesByInput` 表决定；`Primary→X` 必配，`Secondary→Y` 选配。
- 未配 Secondary 时右键单独按**不会攻击**。

### 预输入（提前按）
```
连段中、窗口未开但预输入窗口开 → 按左/右 → BufferAttack(InputTag)
  → 窗口一开 → NotifyComboWindowOpened → RequestAttack(BufferedInputTag) → 自动接下一段
  → 或本招收招 → NotifyComboEnded → 触发缓冲
```

### 翻滚保留连段（+ 翻滚预输入）
```
连段中按翻滚 → Roll(): ClearBufferedAttack + RefreshComboGrace + 激活翻滚
  → 翻滚的 CancelAbilitiesWithTag 取消当前连段 GA → NotifyComboEnded → 开宽限(2s)
  → 翻滚动画中按攻击 → 连段 GA 被 ActivationBlockedTags={Roll} 挡住 → 组件改为 BufferAttack 缓冲
  → 翻滚结束（Roll tag 归零，组件 BeginPlay 里 RegisterGameplayTagEvent 监听）→ FlushBufferedAttack → 自动出下一招
  → 翻滚后（宽限内）按攻击 → 直接树查跳下一段（不回到起手节点）
  → 发呆超宽限 → OnComboReset → CurrentNodeTag 清空（重新起手）
```

---

## 九、Tag 一览（`GamePlayTags/FTTag.h`）

| Tag | 用途 |
|---|---|
| `FFTag.Windows.Combo` | 组合窗口（loose tag）；翻滚的 ActivationRequiredTags 需要它 |
| `FFTag.Combo.X / .XX / .XXX / .XXXX` | 默认 XXXX 路径节点 tag |
| `FFTag.Combo.XY / .XYX / .XYY / .XYYX` | 分支节点 tag（示例，按需继续加） |
| `FFTag.Abilities.Primary` / `Secondary` | 左/右输入（连段树跳转的 key） |
| `FFTag.Abilities.Roll` | 翻滚 |
| `FFTag.Abilities.ActivatedOnGiven` | 给能力时自动激活（ASC 处理） |
| `FFTag.Status.Dead` | 死亡状态（激活守卫用） |
| `FFTag.Events.Player.PrimaryTraceHit` | 命中事件 |
| ~~`FFTag.Abilities.BlockHitReact`~~ / ~~`.Death`~~ | **未使用**，可删 |

---

## 十、不再需要的类 / 资产 / Tag

| 对象 | 现状 | 建议 |
|---|---|---|
| **`GA_Roll.uasset`**（旧翻滚） | 从未给玩家，GA_ActionRoll 的旧副本 | **删除** |
| **`UFT_ComboWindow`**（瞬时 Refresh notify） | 在 X/XX/XXX 蒙太奇上做 `Remove+Add(窗口tag)`；节奏门槛已改读组件标志，它不再起作用 | **从蒙太奇移除并删类** |
| **`GE_RollCombo`** | 翻滚完成时加 `Window.Combo` 0.5s；现在不影响节奏门槛 | **可移除**（删 GA_ActionRoll 里的施加节点 + 删资产） |
| **翻滚蒙太奇的 `UFT_ComboWindowState`** | 已从翻滚蒙太奇移除 | 保持移除 |
| `FFTag.Abilities.BlockHitReact` / `.Death` | 无能力/代码使用 | 删除 tag 定义 |
| `bCanPerInput` / `PerInputTag`（控制器） | 旧预输入残留 | **已删除** |

---

## 十一、当前待办 & 注意点

1. **连段树 DataAsset 已建**：`DA_Serath_Combo`（`UFT_ComboDefinition` 实例）已配好 XXXX 路径并跑通。剩余配置：
   - `StartNodesByInput`：**Primary→`FFTag.Combo.X` 必填**（否则无起手）；要右键起手再配 `Secondary→FFTag.Combo.Y`。
   - `Nodes` 补齐分支/起手节点（每个节点选 GA + 填 `NextByInput`）；Y 起手需先建 `GA_Serath_Combo_Y`（父类 `UFT_ComboGamePlayAbility`、`ComboNodeTag=FFTag.Combo.Y`、AbilityTags、蒙太奇 + notify）。
2. **重启编辑器前必须完整构建（DebugGame，与编辑器加载的 DLL 一致）**：
   ```
   "D:\UEEngine\UE_5.8\Engine\Build\BatchFiles\Build.bat" FirstEditor Win64 DebugGame -Project="D:\Dev\CPP\Unreal Projects\First\First.uproject" -WaitMutex
   ```
3. `ComboResetDelay`（2s）在 `UFT_ComboComponent` UPROPERTY 上，蓝图可改。
4. 翻滚的 `ActivationRequiredTags={Window.Combo}` = 中立状态翻滚不可用（战斗专用闪避），想改就删这项。
5. `GA_ActionRoll` 的 PlayMontageAndWait **建议勾 `bAllowInterruptAfterBlendOut`**（翻滚 tag 残留根因）。
